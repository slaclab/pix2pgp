[build-system]
requires = ["setuptools>=64", "Cython>=3.0", "numpy>=1.20"]
build-backend = "setuptools.build_meta"

[project]
name = "pix2pgp"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = ["numpy", "click"]

# README!

The contents of `vault/` are supposed to be *linked* into the `rtl/` `rtl/pkg` and `rtl/wrapper` directories by the `ghdlRun.sh` script.

One should *not* point an RTL analysis tool into the `vault/` directory.
